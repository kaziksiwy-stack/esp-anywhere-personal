# Security policy

Report vulnerabilities privately through GitHub Security Advisories. Never include live tokens, codes, Wi-Fi credentials or unredacted logs in issues.

After exposure rotate ADMIN_TOKEN; clear NVS and re-provision affected devices; recreate an HA entry if its token leaked. Codes expire in 10 minutes but remain secret while valid.
